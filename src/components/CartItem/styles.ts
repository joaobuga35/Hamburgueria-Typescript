import styled from "styled-components";

export const CartItem = styled.li`
  display: flex;
  img {
    height: 6rem;
    width: 6rem;
    background-color: var(--gray-100);
  }

  h2 {
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--gray-600);
  }
`;
